# Map utilities

This directory is reserved for map-specific helpers such as geometry transforms,
projection utilities, and feature metadata adapters.

Phase 0 keeps the implementation lightweight with a static SVG placeholder map.
